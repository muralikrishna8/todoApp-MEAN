extends layout

block content
    h1= title
    p Welcome to #{title}
    .content(ng-controller= "todoController as todos")
        input(type = "checkbox")
        span(ng-repeat="todo in todos.list") {{todo.text}}
